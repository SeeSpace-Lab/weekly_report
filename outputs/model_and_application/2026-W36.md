# 模型应用周报 · 2026-W36

> 时间窗口：2026-08-30T16:00:00+00:00 — 2026-09-06T15:59:59.999999+00:00
> 目标阅读时间：约 30 分钟

## 本周趋势

- 本周有38项相关进展，集中在多模态模型。

## 本周必读

### 1. [ShallowStream: Index Shallow then Answer Deep for Streaming Video Understanding](https://arxiv.org/abs/2609.02780)

- 推荐理由：ShallowStream 从模型深度而非仅 token 数量削减流视频成本，并同时解决持续索引与查询时证据检索。
- 部门意义：可将浅层常驻索引用于长视频和遥感时序筛查，只让少量证据进入深层 VLM。
- 时间：2026-09-02T16:14:48+00:00
- 建议阅读：4 分钟

**中文标题：** ShallowStream：浅层持续索引、深层按需回答

**系统用 MLLM 浅层 KV 构建常驻视频索引，查询时依据浅层注意力和多样性策略选择证据帧，再进入深层回答。**

流视频若对每个新帧反复执行完整深度 prefill，计算开销巨大且 KV Cache 按深度持续增长；仅在编码后剪 token 无法消除前端成本。

ShallowStream 只运行浅层完成帧编码和索引，保存浅层 KV；收到问题后用浅层注意力给上下文帧打分，并用多样性感知策略检索证据。

作者报告性能与最强流式方法相当，同时每帧 prefill 延迟最高降低 52.1 倍，10 秒端到端延迟最高降低 11.9 倍。

### 2. [SinkPruner: Sink-Free Visual Token Pruning for Multimodal Large Language Models](https://arxiv.org/abs/2609.01004)

- 推荐理由：SinkPruner 指出高范数 attention sink 被误当信息 token 的系统性问题，并给出无需训练、可迁移的粗到细剪枝机制。
- 部门意义：视觉 token 压缩实验应先诊断 attention sink，再比较语义剪枝；该方法可作为不训练基线。
- 时间：2026-09-01T09:52:10+00:00
- 建议阅读：4 分钟

**中文标题：** SinkPruner：先去 Attention Sink，再做文本引导剪枝

**方法先过滤特征与空间上冗余的高范数异常 token，再保留与查询语义对齐的视觉 token。**

现有视觉 token 剪枝常把高范数异常 token 误判为重要线索，造成 attention sink 和注意力扩散，损害压缩决策。

SinkPruner 使用 visual sanitizer 清除高范数冗余，再用 text-guided pruner 做查询相关细筛，全流程无需训练。

在 12 个图文和 4 个视频语言基准上，LLaVA-1.5 与 Qwen2.5-VL 在减少 89% token 时分别保留 96.5% 与 91.8% 原始性能；sanitizer 还能迁移增强其他剪枝方法。

### 3. [Compressing AI Traffic: Standardized Neural Network Coding of Visual-Token Representations in Split Vision-Language Inference](https://arxiv.org/abs/2609.01200)

- 推荐理由：该工作把分离式 VLM 的中间视觉 token 定义为 AI traffic，并用标准化编码器证明任务性能对极强有损压缩的容忍度。
- 部门意义：可把中间视觉 token 作为新型码流，以任务准确率直接约束码率，并验证标准 NNC 对遥感分离式推理的适配。
- 时间：2026-09-01T13:07:32+00:00
- 建议阅读：4 分钟

**中文标题：** Compressing AI Traffic：分离式 VLM 视觉 Token 的标准编码

**研究在 Qwen3-VL-8B 的完整视觉接口插入 ISO/IEC 15938-17 NNC 编解码，探索中间表示的码率—任务性能边界。**

视觉编码器与语言解码器跨节点部署时，主视觉 token 和 DeepStack 特征成为高带宽通信负载，传统率失真目标未必对应问答质量。

作者保持权重、提示和生成不变，对完整视觉接口执行标准 NNC 往返并扫描量化参数，在 Video-MME 与 MLVU 上测任务性能。

BF16 张量传输量减少 98% 前，闭式准确率仍接近未压缩基线，开放式生成也呈相同的平台—崩塌曲线；任务对明显有损的中间表示仍保持韧性。

## 核心主题

### 1. [Exploring the Potential of Contrastive Language-Image Pre-training for Multi-Source Remote Sensing Data](https://arxiv.org/abs/2609.03391)

- 推荐理由：OmniRSCLIP 直面 RGB、SAR、多光谱和高光谱的异构输入接口，并发布多源语图语料。
- 部门意义：可将波长条件基重组作为多源遥感 VLM 的输入适配基线，并评估统一空间是否牺牲传感器特有信息。
- 时间：2026-09-03T05:45:31+00:00
- 建议阅读：4 分钟

**中文标题：** OmniRSCLIP：面向多源遥感传感器的统一语图预训练

**方法用光谱—空间基分解把任意通道传感器映射到 CLIP 预训练视觉先验，并统一对齐 RGB、SAR、MSI 与 HSI。**

RGB 导向的 CLIP 固定输入通道难以利用 SAR、多光谱和高光谱数据，直接改输入又容易破坏预训练视觉知识。

SSBD 将任意通道适配写成基重组：预训练 patch embedding 提供空间基，波长条件系数组合传感器特定核；再用光谱上下文 mask 对比学习抑制模态冗余。

作者称在检索、零样本分类和语义定位中保留强 RGB 性能，同时有效扩展到异构传感器；并构建覆盖四类模态的 OmniRS5M 语图语料。

### 2. [Agentic Multimodal Models for Environmental Hyperspectral Unmixing](https://arxiv.org/abs/2609.01289)

- 推荐理由：该工作让 Agent 调工具检查并修正端元集合，而非让 LVLM 替代数值遥感算法，提供更可信的遥感 Agent 组合范式。
- 部门意义：遥感 Agent 可优先采用数值算法、可审计工具与有限编辑动作组合，避免把物理约束完全交给语言模型。
- 时间：2026-09-01T14:24:35+00:00
- 建议阅读：4 分钟

**中文标题：** 工具型 LVLM Agent 改进高光谱解混流程

**Agent 在传统模型阶数、端元提取和丰度估计流水线上，通过谱库检索与丰度图可视化迭代合并或删除端元。**

模块化高光谱解混的前级误差会产生冗余或含混端元并向丰度估计传播；端到端替换又弱化物理可解释性。

算法无关 Agent 从初始分解出发，调用谱库检索、丰度图可视化等工具收集光谱和空间证据，对端元集合执行 merge/discard，再重估丰度。

在 HYDICE Urban、Jasper Ridge 与 Stonewall Playa 上，Agent 对多种模块组合持续改善端元数量，并普遍改善光谱签名与丰度图，与 CNN-AE、uDAS、R-CoNMF 保持竞争力。

## 次核心主题

### 1. [ViSAR: Training-Free Adaptive-$k$ Retrieval for Visual Document Question Answering](https://arxiv.org/abs/2609.02486)

- 推荐理由：ViSAR 以查询复杂度自适应决定视觉文档页数，展示不训练条件下同时降低输入 token 与延迟的检索机制。
- 部门意义：可把自适应证据量用于多页报告、遥感产品说明和 Agent 工具返回，按问题难度控制视觉上下文。
- 时间：2026-09-02T11:54:58+00:00
- 建议阅读：3 分钟

**中文标题：** ViSAR：视觉文档问答的自适应 top-k 检索

**方法直接在 late-interaction 嵌入空间构造查询条件的页级相似矩阵，动态决定送入 LVLM 的页面数。**

DocVQA 的固定 top-k 不考虑问题复杂度，简单问题读入过多页面会增加延迟并可能降低答案准确率。

ViSAR 从查询—页面交互构造视觉语义激活矩阵，识别相关语义并训练自由地选择随查询变化的页面集合。

跨多种编码器和 LVLM，RAG 延迟最高降低 58.7%，答案准确率相对固定 top-k 与自适应启发式保持或提高；矩阵结构还与准确率相关。

### 2. [CoFiE: Coarse-to-Fine Evidence Selection for Efficient Streaming Video Understanding](https://arxiv.org/abs/2609.03675)

- 推荐理由：CoFiE 把证据筛选前移到视觉编码器之前，再在 LLM prefill 阶段细化，补足编码后 token 剪枝无法节省前端成本的问题。
- 部门意义：视频压缩与 VLM 系统应联合优化帧级和 token 级选择，可用遥感时序变化新颖度作为编码前信号。
- 时间：2026-09-03T11:14:21+00:00
- 建议阅读：4 分钟

**中文标题：** CoFiE：编码前粗筛、Prefill 时细选流视频证据

**框架先用新颖度无关查询地过滤帧，再在问题到来后选择最相关证据，从两端压缩流视频处理。**

多数 token 剪枝和记忆库方法在视觉编码之后才减量，昂贵帧编码已经发生，因此端到端延迟收益受限。

Novelty-Guided Frame Filtering 在编码前保留视觉上独特的候选帧，Query-Specific Evidence Refinement 在 LLM prefill 中按问题再筛。

StreamingBench 达 78.86%，OvO-Bench 达 68.72%，相对既有方法最高提高 3.15%；过滤最多 80% 证据帧时仍优于强开源模型，端到端延迟最高改善 2.54 倍。

## 框架、Benchmark 与数据集

### 1. [Principia: Relational Physics Tests for Video Models](https://arxiv.org/abs/2609.04200)

- 推荐理由：Principia 用同场景对象间关系规避帧率、尺度和相机标定歧义，为视频模型物理推理提供更严格的评测轴。
- 部门意义：视频模型与世界模型评测应增加关系约束；遥感变化生成也可借鉴同场景成对一致性。
- 时间：2026-09-03T17:59:50+00:00
- 建议阅读：3 分钟

**中文标题：** Principia：用关系一致性测试视频模型的牛顿物理

**基准以成对对象在同一物理规律下必须满足的相对关系，在图像空间评估八类动力学现象。**

绝对运动测量依赖帧率、物体尺度和相机标定，生成视频常缺少这些信息，传统质量基准难以判定物理正确性。

Principia 在受控真实场景记录重力、恢复系数、摩擦、转动惯量、抛体、动量、摆和弹簧振荡，并设计不依赖标定的关系一致性分数。

六个先进视频生成器在 VBench 约 0.8，但 Principia 均不超过 0.42；VLM 检测关系物理违规的最好准确率仅 67%，多数接近随机。

---

本周报由自动化情报流水线生成，录用状态与关键判断在发布前仍需人工复核。
