# 模型应用周报 · 2026-W35

> 时间窗口：2026-08-23T16:00:00+00:00 — 2026-08-30T15:59:59.999999+00:00
> 目标阅读时间：约 30 分钟

## 本周趋势

- 本周尚未形成显著趋势簇。

## 核心主题

### 1. [Multi-Image Visual Token Pruning in Large Visual Language Models](https://arxiv.org/abs/2608.26806)

- 推荐理由：AVTP 直接解决多图 LVLM 的动态视觉 token 预算，并兼顾 FlashAttention 兼容性。
- 部门意义：可作为多图遥感 VLM 的 token 预算基线，重点比较固定比例、按图重要性和任务条件化三种策略。
- 时间：2026-08-27T00:00:00+00:00
- 建议阅读：4 分钟

**中文标题：** AVTP：面向多图 LVLM 的自适应视觉 Token 裁剪

**该训练自由框架按图像重要性动态分配视觉 token，在三类 LVLM 上保持精度并降低推理成本。**

现有视觉 token 裁剪多采用静态策略，难适配不同 LVLM 架构和多图场景的重要性差异；依赖显式 attention 的实现还可能与 FlashAttention 冲突。

AVTP 根据不同 LVLM 的视觉注意分布经验选择裁剪层，再估计多图输入中各图的相对重要性，将总 token 预算按重要性自适应分配；全流程无需训练或附加模型。

官方摘要报告 Qwen3VL-8B 获得 2 倍推理加速并保持 96.1% 原准确率，InternVL3.5-8B 保持 94.1%，LLaVA-OV-7B 甚至略高于未裁剪基线。

### 2. [HAP: Head-Adaptive Visual Token Pruning via Cross-Modal Alignment](https://arxiv.org/abs/2608.23921)

- 推荐理由：HAP 从注意头与提示词—图像区域对齐质量出发，比简单平均注意力更贴近视觉 token 的任务价值。
- 部门意义：值得在遥感 VLM 上测试 PAQ 是否能保护小目标和稀疏关键区域，并与 AVTP 的按图预算组合。
- 时间：2026-08-24T00:00:00+00:00
- 建议阅读：4 分钟

**中文标题：** HAP：用跨模态对齐质量选择视觉 Token

**HAP 以 PAQ 给注意头加权，并联合层组 FLOPs 预算完成细粒度视觉 token 保留。**

把所有注意头均匀平均会让与查询不对齐的头放大背景 token，削弱真正与提示词相关的细粒度区域。

方法提出 PAQ 衡量每个注意头对提示词和图像区域的对齐质量；先把层划分为组并按 FLOPs 目标分配 token 预算，再以 PAQ-softmax 聚合注意图并在各组保留高分 token。

摘要称在 18 个基准上取得领先的精度—成本权衡；LLaVA-1.5-7B 在 9 个任务仅保留 5.6% token 时保持 99.1% 原性能，比 AutoPrune 高 4.2 个点。

### 3. [E2S-Pruner: Progressive Two-Stage Evidence Fusion for Visual Token Pruning in Vision-Language Models](https://arxiv.org/abs/2608.23253)

- 推荐理由：E2S-Pruner 对注意头不确定性和跨层冲突做显式建模，为可靠 token 压缩提供不同于排序分数的路线。
- 部门意义：可作为遥感小目标保护和不确定性驱动压缩的候选，对比仅按 attention magnitude 的方法。
- 时间：2026-08-24T00:00:00+00:00
- 建议阅读：3 分钟

**中文标题：** E2S-Pruner：两阶段证据融合的视觉 Token 裁剪

**该方法用三态证据、Dempster–Shafer 融合和空间新颖性约束减少误删与区域集中。**

直接聚合多个注意头和层无法区分证据清晰度、冲突与不确定性，容易把局部显著性误当作全局重要性。

第一阶段将各注意头作为独立证据源，按清晰度和头间一致性估计可靠性，将 token 表示为重要、不重要或不确定；第二阶段用 Dempster–Shafer 理论融合跨层证据，并用空间新颖性保证区域覆盖。

摘要报告 LLaVA-1.5-7B 保留 192/128/64 token 时分别维持 98.0%/96.8%/90.6% 综合性能，128 和 64 token 配置吞吐提升 1.96 倍与 2.09 倍，并在 Qwen2-VL-7B 上验证泛化。

### 4. [Visual Information-Guided Parallel Decoding for Diffusion Multimodal Large Language Models](https://arxiv.org/abs/2608.26580)

- 推荐理由：VIG-Sampler 针对扩散式多模态语言模型的并行解码选择，提供了不同于自回归模型的新推理控制点。
- 部门意义：应把扩散式 MLLM 纳入模型应用路线评估，并单独测量视觉引导排序的成本、批处理友好性和质量稳定性。
- 时间：2026-08-27T00:00:00+00:00
- 建议阅读：4 分钟

**中文标题：** VIG-Sampler：视觉信息引导的扩散式多模态并行解码

**采样器优先解码高图像注意 token，并抑制与已选 token 视觉分布重复的候选。**

扩散式 MLLM 从全 masked 序列逐步解码，每一步选择的 token 会成为后续上下文；纯置信度策略偏向训练频次高的 token，通用信息增益策略又忽略输入图像。

VIG-Sampler 依据候选 token 对图像 token 的注意力排序，并对与已选 token 具有相似图像注意分布的候选加惩罚，使同一步并行解码覆盖互补视觉信息。

官方摘要报告在 3 个开源 dMLLM、7 个 caption/VQA 基准上，相比 Info-Gain Sampler 的 caption 平均提升 19.3 CIDEr；在 COCO Caption 上用一半解码步数取得更好表现。

### 5. [Interpreting Latent Protein Language Model Features with Geometric Annotations](https://arxiv.org/abs/2608.26419)

- 推荐理由：该工作把蛋白语言模型的稀疏特征解释从蛋白级标签推进到残基级几何结构，契合 AI4S 可解释性。
- 部门意义：AI4S 路线可把几何 SAE 注释作为蛋白模型解释基线，并检查其对候选筛选和结构任务的实际增益。
- 时间：2026-08-26T00:00:00+00:00
- 建议阅读：3 分钟

**中文标题：** 用几何注释解释蛋白语言模型的稀疏潜在特征

**作者以蛋白 Cα 骨架几何特征自动注释 ESM-2 的 SAE 神经元，并用接触图消融验证方向性影响。**

现有 SAE 注释多依赖数据库蛋白级标签或对高激活序列的 LLM 描述，容易遗漏局部残基几何与未注释宏基因组序列中的结构信号。

方法为 ESM-2 8M 各层 SAE 特征计算几何启发的 Cα 骨架描述符，使用 FDR 控制的发现分析寻找显著关联，并对接触预测执行特征级消融。

摘要报告许多 SAE 特征与局部几何显著相关，几何可区分拥有同一数据库标签的不同特征；消融这些特征会让 ESM-2 接触图沿对应几何描述符方向变化。

### 6. [Unlocking Multimodal Protein Language Models at Inference Time](https://arxiv.org/abs/2608.25855)

- 推荐理由：论文系统比较多模态蛋白语言模型的推理时采样、logit 引导与轨迹搜索，能改变 AI4S 的实验设计。
- 部门意义：AI4S 实验不应只比较 checkpoint；应把采样、CFG 和搜索预算作为一等变量，并统一报告质量—计算 Pareto 曲线。
- 时间：2026-08-26T00:00:00+00:00
- 建议阅读：4 分钟

**中文标题：** 多模态蛋白语言模型的推理时扩展：采样、引导与搜索

**该研究在三个蛋白模型和四类任务上比较默认采样、classifier-free guidance 与 reward-guided beam search。**

蛋白生成研究长期侧重训练模型，却缺少对推理阶段探索—利用权衡、逐步 logit 控制和多轨迹搜索的系统比较。

作者构建三阶段推理设计空间：调整采样分布、用任务特定 classifier-free guidance 修改逐步 logits、用奖励引导 beam search 管理并行轨迹，并在三个代表性多模态 pLM 和四项基础任务上评估。

摘要报告默认推理协议并非最优，不同任务偏好不同采样策略；在不更新模型参数的前提下，多种推理控制可稳定提高各任务的上限表现，并导致对基础模型能力排序的结论不同于既有共识。

### 7. [MolEmb: Multimodal Large Language Models Can Be Strong Molecular Embedding Models](https://arxiv.org/abs/2608.23646)

- 推荐理由：MolEmb 将多模态大模型转为可按自然语言上下文条件化的分子 embedding 基座，连接 VLM 与 AI 制药。
- 部门意义：可用于评估通用 MLLM 是否能替代专用分子 encoder，但数据设计必须显式覆盖要控制的药物语义上下文。
- 时间：2026-08-24T00:00:00+00:00
- 建议阅读：3 分钟

**中文标题：** MolEmb：把多模态大模型适配为上下文感知分子 Embedding 模型

**该框架用双向对比目标对齐分子多视图与文本描述，并引入上下文检索诊断基准 MolCAR。**

传统分子 encoder 多针对单一分子视图，输出不随语义任务变化的固定向量，难统一支持性质预测、虚拟筛选和跨模态检索。

MolEmb 让 MLLM 接收图像、文本和符号化分子 profile，以双向对比学习将分子与文本描述对齐到共享空间；MolCAR 用来诊断 embedding 是否真正响应自然语言上下文。

官方摘要称 MolEmb 在分子性质预测上具有竞争力，并在同一空间支持 molecule–text 双向检索；诊断结果表明上下文感知能力主要取决于监督数据是否包含相应语义条件。

### 8. [Semantic Compression Trees: Multi-Resolution Knowledge Retrieval via Hierarchical Semantic Residuals](https://arxiv.org/abs/2608.21610)

- 推荐理由：SCT 对语义压缩的正负结果都给出统计检验，能避免把层次结构本身误当作检索收益。
- 部门意义：语义压缩研究应分别报告表示、路由和任务答案质量；可先复用残差表示，再替换全局路由器。
- 时间：2026-08-21T00:00:00+00:00
- 建议阅读：3 分钟

**中文标题：** 语义压缩树：语义残差值得保留，树形路由仍失败

**SCT 通过节点语义残差压缩知识，却发现最压缩的根节点不足以承担全局文档选择。**

固定粒度 RAG 索引丢失层次结构且检索成本随集合增长；但层次压缩可能在顶层损失决定文档路由的关键信息。

每个节点只保存超出父节点的信息增量，查询按树逐层下降；作者在 QASPER 的给定文档和全库选文档协议上，使用 bootstrap 置信区间及配对显著性检验分离残差表示与路由效果。

给定文档时以 30% 更少上下文 token 达到 0.274 F1，对比 dense 0.277（p=0.37）；但全库选文档时树下降为 0.122，低于平坦检索 0.165，正确文档路由率仅 20.2%，平坦方案为 39.3%。

---

本周报由自动化情报流水线生成，录用状态与关键判断在发布前仍需人工复核。
